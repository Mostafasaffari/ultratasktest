const handleError = (err: any) => {
  //TODO: we can implement this method in a correct way base on backend structure for errors

  console.log("show error message or redirect to somewhere");
};

export default handleError;
